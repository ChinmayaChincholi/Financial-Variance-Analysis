# Development-only dependencies -- needed to build and test this
# project, but NOT bundled into the packaged desktop app.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
openpyxl==3.1.5   # only used by tests, to read back exported .xlsx
                  # files and verify their contents (pd.read_excel).
                  # The app itself only WRITES Excel files (via the
                  # xlsxwriter engine) and never reads them, so this
                  # isn't needed in the packaged app -- yet. If a
                  # future feature needs to READ .xlsx (e.g. importing
                  # a client's exported file back in), move this back
                  # to requirements.txt at that point.